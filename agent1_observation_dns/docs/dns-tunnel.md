# DNS tunnel specialist

The fast lexical/context branch runs first. Byte-CNN, ordered metadata, and optional relation branches run only for suspicious-band traffic, structural triggers, audit samples, or research evaluation. Every skipped branch remains in the fusion evidence. The relation branch requires sufficient observed client/domain/resolver edges and never infers invisible answer relationships.
