# ScenarioRelease v1

Scenario plans carry a reproducible seed, exact UTC interval, endpoints, software versions, parameters, labels, split group, tool, hard-negative tags, and feature snapshots. Partitions are assigned before frame generation. Release creation refuses duplicate IDs, provenance crossing, and overwrite.
