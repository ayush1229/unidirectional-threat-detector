# DGA specialist

The character branch provides a compact masked character attention model and CNN comparison. The context branch uses visible DNS/context values plus explicit availability masks. Training requires provenance-aware train/validation/test partitions and can evaluate dictionary, human-looking, adversarial, time, and held-out-family cohorts. Without a trained artifact, the documented lexical baseline is uncalibrated and carries uncertainty `1.0`.
