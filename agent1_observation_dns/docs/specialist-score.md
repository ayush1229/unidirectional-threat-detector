# SpecialistScore v2

Scores are specialist evidence, never a final decision. A skipped or unavailable branch has `probability: null`, `score_present: false`, and a reason such as `EARLY_EXIT` or `CHECKPOINT_ABSENT`. The fusion object retains every component and never averages unavailable scores.
